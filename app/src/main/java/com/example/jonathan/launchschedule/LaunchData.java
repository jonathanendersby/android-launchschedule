package com.example.jonathan.launchschedule;

import java.util.ArrayList;
import java.util.List;

public class LaunchData {
    // Holds the launches list so that various classes/activities can access it.

    static List<Launch> launches = new ArrayList<Launch>();
}